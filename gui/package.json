{
  "name": "axle-gui",
  "version": "1.0.0",
  "description": "Axle GUI - Electron application with TypeScript",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "electron .",
    "dev": "npm run build && electron ."
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "electron": "^30.0.0",
    "@types/node": "^20.0.0"
  }
}
